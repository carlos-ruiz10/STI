@model IEnumerable<STI.Models.temas_curso>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.nombre_tema)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.curso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_tema)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.curso.nivel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_temas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_temas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_temas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
